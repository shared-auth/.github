# Credential-free organization policy profile.
SHARED_AUTH_ENV_POLICY_STRICT=true
SHARED_AUTH_COMMUNICATIONS_POLICY_STRICT=true

# The .github repository must not own provider, database, signing, service-role,
# bearer-token, deployment, or cloud credentials.
